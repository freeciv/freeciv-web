set compresstype xz
lock compresstype
set maxconnectionsperhost 256
lock maxconnectionsperhost

# Should we disable also nuclear winter?
# It's largely the same code which caused the problems with global warming
# TODO: This has not been re-evaluated soon in ten years.
#       Does freeciv-web still need this?
set globalwarming disabled
lock globalwarming
